{
  "name": "@note-maps/models",
  "version": "0.1.0",
  "description": "Note Maps models for communication between related abstractions",
  "main": "dist/index.js",
  "repository": "https://github.com/google/note-maps",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "test": "ts-mocha -p tsconfig.json src/**/*.spec.ts"
  },
  "devDependencies": {
    "mocha": "^7.1.2",
    "ts-mocha": "^7.0.0"
  }
}
